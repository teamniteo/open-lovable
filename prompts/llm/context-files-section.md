Source: lib/context-selector.ts (buildSystemPrompt → Context Files section)
Tool: AI SDK (provider-agnostic)

Template:

## Context Files (for reference only)
- ${filePath}${componentInfo ? ` (${componentName} component)` : ''}

